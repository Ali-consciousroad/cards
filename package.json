{
  "name": "build-a-fragment",
  "version": "1.0.0",
  "description": "workspace for the Build an HTML Fragment exercise",
  "scripts": {
    "start": "live-server --port=3000"
  },
  "dependencies": {
    "live-server": "^1.1.0"
  }
}
